module.exports = {
    name: "basically",
    description: "Okay, so basically......",
    guildOnly: true,
    execute(message, args) {
      message.channel.send(`Okay, so basically, in essence, overall, in conclusion, basically really kind of actually generally really sort of really pretty basically kind of fairly definitely kind of really generally sort of contrary to popular belief, which for the most part literally definitely really for all intents and purposes really generally actually specifically literally for the most part literally actually particularly basically is fairly significant, which mostly kind of mostly specifically kind of kind of definitely essentially basically actually really mostly essentially for all intents and purposes really is fairly significant in a fairly actually generally pretty really fairly kind of for all intents and purposes pretty really sort of big way in a subtle way in a pretty for all intents and purposes really fairly really definitely sort of generally very actually pretty big way, kind of fairly really sort of for all intents and purposes actually actually very particularly definitely particularly pretty contrary to popular belief in a actually particularly actually pretty definitely particularly definitely actually particularly very really major way, or so they generally kind of actually kind of mostly actually for all intents and purposes specifically for all intents and purposes for all intents and purposes thought in a subtle way, which definitely kind of particularly kind of definitely specifically generally is fairly significant in a very very really actually actually basically fairly big way in a generally basically fairly sort of generally big way, which for all intents and purposes for the most part definitely particularly mostly is quite significant in a subtle way, or so they particularly thought, which essentially particularly is fairly significant, which generally is quite significant this proves that, so particularly, fairly significantly, so basically, i’m monkey.`);
    }
  };